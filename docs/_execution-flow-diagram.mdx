import DiagramMotion from '@site/src/components/DiagramMotion';

At a glance, every cross-rollup call goes through three stages:

<DiagramMotion
  src="/motion/execution-flow.webm"
  poster="/motion/execution-flow-poster.png"
  alt="A cross-rollup call runs in three phases: off-chain prove and queue, execute on L1, then execute independently on L2."
  caption="Three phases: off-chain prove & queue → execute on L1 → execute independently on L2."
/>

The full call, contract by contract, for `HelloWorldL1.helloL2World()` calling through to `HelloWorldL2.getWord()`:

<DiagramMotion
  src="/motion/actor-graph.webm"
  poster="/motion/actor-graph-poster.png"
  alt="Contract-by-contract call trace across L1 and L2: green arrows are calls in order, mint dashed arrows are returns, and the proof is checked at the EEZ Registry."
  caption="The full call, contract by contract — green = calls (in order), mint dashed = returns."
/>

Solid green arrows are calls, dashed mint arrows are returns (mint marks a settled/proven value in the EEZ visual grammar). `EEZ.postAndVerifyBatch()` verifies the proof and queues the entry; `EEZ.executeCrossChainCall()` checks the hash and applies the state delta. Both sides happen in the same L1 block — see [Execution flow: a complete example](#execution-flow-a-complete-example) below for the step-by-step breakdown.
